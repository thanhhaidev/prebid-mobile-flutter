name: prebid_mobile_sdk_admob
description: "Google AdMob mediation for the Prebid Mobile Flutter SDK, via Prebid's AdMob adapters. Optional companion to prebid_mobile_sdk."
version: 0.0.1
homepage: https://github.com/thanhhaidev/prebid-mobile-flutter
repository: https://github.com/thanhhaidev/prebid-mobile-flutter
issue_tracker: https://github.com/thanhhaidev/prebid-mobile-flutter/issues

topics:
  - ads
  - prebid
  - admob
  - mediation
  - header-bidding

resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: '>=3.41.5'

dependencies:
  flutter:
    sdk: flutter
  # Core Prebid plugin (sibling workspace package). Switch to a hosted version
  # constraint (prebid_mobile_sdk: ^x.y.z) before publishing.
  prebid_mobile_sdk:
    path: ../prebid_mobile_sdk

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.prebid.prebid_mobile_sdk_admob
        pluginClass: PrebidMobileSdkAdmobPlugin
      ios:
        pluginClass: PrebidMobileSdkAdmobPlugin
